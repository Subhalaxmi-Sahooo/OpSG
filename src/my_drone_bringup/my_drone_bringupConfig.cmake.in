# - Config file for the my_drone_bringup package
# It defines the following variables
#  my_drone_bringup_INCLUDE_DIRS - include directories for FooBar
#  my_drone_bringup_LIBRARIES    - libraries to link against
#  my_drone_bringup_EXECUTABLE   - the bar executable

set(my_drone_bringup_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${my_drone_bringup_DIR}/export_my_drone_bringup.cmake")

# These are IMPORTED targets created by my_drone_bringupTargets.cmake


